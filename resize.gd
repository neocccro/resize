extends Node


# Called when the node enters the scene tree for the first time.
func _ready():
	#DisplayServer.window_set_min_size(Vector2i(200, 200) )
	#DisplayServer.window_set_max_size(Vector2i(400, 400) )
	#DisplayServer.window_set_size(Vector2i(300, 300) )
	#var cally = Callable(self, "_on_viewport_size_changed")
	#get_viewport().connect("size_changed", cally)
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
